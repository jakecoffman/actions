name: Blah
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:
jobs:
  server:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: echo "::warning::Missing semicolon"
      - run: echo "::error title=error testing::Does not compute"
      - run: pigz
